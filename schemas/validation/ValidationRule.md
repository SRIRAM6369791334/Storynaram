# ValidationRule

**File:** `ValidationRule.schema.json`

**Purpose:** Defines a single validation rule with condition, severity, and message.

**Type:** Standalone Validation Schema

**Template Source:** `templates/validation/ValidationRule.template.json`

**Required Properties:** `name`, `condition`, `severity`, `message`

**Key Enums:** `severity` (error, warning, info)

**Validation Scope:** field

**Cross-References:** ValidationProfile, ValidationResult
